# 内容校验系统

> 返回：[[00 后端项目亮点分模块索引]]
> 相关代码：`backend/app/graphs/nodes/validator_nodes.py`、`backend/app/graphs/nodes/validator_experience.py`

## 它解决什么问题

大模型输出不能直接给用户。尤其在心理咨询场景，要检查：

- 是否泄露诊断。
- 是否给用药或危险方法。
- 是否承诺绝对保密。
- 是否冒充真人/医生/咨询师。
- 是否强化依赖。
- 是否复制 RAG 原文。
- 是否过度心理化。
- 是否问题太多。
- 是否忽略用户当前锚点。
- 是否胡编文化作品细节。

## 两类校验

### 安全校验

位于 `validator_nodes.py`，检查硬性安全问题：

- diagnosis leak
- medication leak
- dangerous method
- delusion confirmation
- dependency reinforcement
- treatment promise
- absolute confidentiality
- role impersonation
- unverified resource
- unsafe button
- RAG copy leak

这些通常是 blocking。

### 体验校验

位于 `validator_experience.py`，检查体验质量：

- banned phrase。
- moralizing reassurance。
- professional referral first turn。
- length budget exceeded。
- too many questions。
- repeated safety question。
- reused formulaic opening。
- ignored topic anchor。
- over psychologizing。
- generic buttons。
- conversation restart。
- fabricated cultural claim。
- overconfident cultural claim。
- reused reply structure。
- missed primary lane。
- violated voice contract。
- failed short-term adaptation。

体验校验会区分 warning 和 block。

## 重写机制

`response_validator` 如果发现 blocking reasons，会尝试调用模型重写：

```text
validator failed
  -> build repair prompt
  -> deepseek_client.chat
  -> parse reply/actions
  -> re-run validator
  -> pass / failed_no_reply
```

如果重写仍失败，就返回 `failed_no_reply`，避免把有问题的回复发给用户。

## 为什么是亮点

内容校验不是单纯关键词过滤，而是结合了：

- 安全策略。
- conversation move policy。
- 用户近期纠偏。
- 文化锚点证据。
- 回复结构复用。
- RAG 防复制。

## 对外讲法

> validator 同时做安全闸门和体验闸门。它不只是拦危险词，还会检查过度心理化、追问过多、忽略用户锚点、胡编作品细节等心理陪伴产品中特有的问题。

## 开发边界

- 安全规则新增后，要确认是否 blocking。
- 体验规则新增后，要确认 warning/block 严重度。
- 修改重写 prompt 时，要防止暴露内部校验原因给用户。

## 推荐验证

```powershell
cd E:\心理咨询agent\backend
.\.venv\Scripts\python.exe -m pytest tests/test_conversation_control_rag.py tests/test_backend_refactor_contract.py -q
```
