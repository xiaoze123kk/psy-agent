# 对话走向策略

> 返回：[[00 后端项目亮点分模块索引]]
> 相关代码：`backend/app/services/conversation_move_policy.py`、`backend/app/services/conversation_policy_anchors.py`、`backend/app/services/conversation_policy_adaptation.py`、`backend/app/services/conversation_policy_structure.py`

## 它解决什么问题

很多心理陪伴 agent 的问题不是“不安全”，而是“不像人在对话”：

- 每轮都“听起来你……”。
- 每轮都分析成创伤、压抑、防御。
- 用户说“别分析我”，下一轮还继续分析。
- 用户提了一个具体作品/人物/隐喻，回复却泛化成普通情绪。
- 问题太多，像问卷。

这个项目用 `conversation_move_policy` 来处理这些体验层问题。

## 主要输出

它会生成 `conversation_move_policy`，其中包括：

- `conversation_move`
- `intent_lanes`
- `primary_lane`
- `ningyu_voice_contract`
- `adaptation_state`
- `topic_anchor`
- `anchor_value`
- `anchor_evidence`
- `structure_mode`
- `structure_style`
- `psychologizing_risk`
- `button_style`

这些字段会影响 prompt 和 validator。

## 锚点识别

`conversation_policy_anchors.py` 会识别：

- 文学作品，如《德米安》《在轮下》。
- 哲学人物或概念，如荣格、尼采、存在主义。
- 影视/媒体角色。
- 人物 anchor。
- 日常细节，如花、包子、咖啡。
- 隐喻，如“被推着走”“奔跑”“碾死”。

识别到锚点后，系统会更倾向于直接回应用户提到的具体东西，而不是泛泛地说“你很难受”。

## 短期适配

`conversation_policy_adaptation.py` 会读取最近 assistant metadata 和用户纠偏信号，例如：

- “别分析我”
- “不要一直问”
- “别问安全”
- “不是这个意思”
- explicit feedback：too_analytic、too_many_questions、too_generic、missed

然后生成短期状态：

- `avoid_analysis_turns`
- `avoid_questions_turns`
- `avoid_safety_check_turns`
- `prefer_direct_anchor_response_turns`

这让系统能在被用户纠正后真正改变，而不是只道歉一句。

## 回复结构变化

`conversation_policy_structure.py` 会检测最近回复结构，避免连续复用：

- 两段式整理 + 追问。
- 单段。
- 短答。
- 停顿式邀请。

这样能减少“每轮都一个格式”的机械感。

## 为什么是亮点

这个模块把“好的对话手感”编码成了可解释策略。它不是安全必须项，但非常影响用户是否愿意继续聊。

## 对外讲法

> 项目单独做了一层 conversation move policy，用来控制每一轮的对话动作、声线、问题预算、分析深度和结构变化。它让 agent 不只安全，还更会接话。

## 开发边界

- 改锚点识别要检查 validator 的文化类校验。
- 改短期适配要跑用户纠偏相关测试。
- 改结构策略要防止 prompt 和 validator 判断不一致。

## 推荐验证

```powershell
cd E:\心理咨询agent\backend
.\.venv\Scripts\python.exe -m pytest tests/test_conversation_move_policy.py tests/test_dialogue_prompt_builder.py -q
```
